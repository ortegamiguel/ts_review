// Types
const framework: string = 'Angular';
const isProduction: boolean = true;
const PORT: number = 3000;
const styles: Array<string> = ['sasas'];
const sayHello: Function = (name: string) => `Hello ${name}`;

const value = sayHello('Miguel');
console.log(value);
